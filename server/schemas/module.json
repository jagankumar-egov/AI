{
  "type": "string",
  "description": "Basic module information and configuration",
  "title": "Module Configuration",
  "examples": [
    "tradelicence",
    "propertytax", 
    "watercharge",
    "birthcertificate",
    "deathcertificate"
  ],
  "pattern": "^[a-z]+$",
  "minLength": 1,
  "maxLength": 50,
  "documentation": {
    "description": "The module name identifies the specific service module within the system. This is a required field that must be a lowercase string value without spaces or special characters.",
    "usage": "Use this field to specify which module the service belongs to. Common modules include tradelicence, propertytax, watercharge, etc.",
    "validation": "Must be a lowercase string with only letters, no spaces or special characters allowed.",
    "examples": {
      "valid": ["tradelicence", "propertytax", "watercharge"],
      "invalid": ["Trade License", "PROPERTY_TAX", "water-charge"]
    }
  }
} 