{
  "type": "string",
  "description": "Service configuration for the application",
  "title": "Service Configuration",
  "required": true,
  "examples": ["TradeLicense", "PropertyTax", "WaterCharge"],
  "documentation": {
    "description": "The service name defines the specific service being configured",
    "helperText": "Enter the service name (PascalCase)",
    "required": true
  },
  "pattern": "^[A-Z][a-zA-Z]+$",
  "minLength": 3,
  "maxLength": 20
} 