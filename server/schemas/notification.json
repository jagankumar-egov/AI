{
  "type": "object",
  "description": "Notification settings and templates",
  "title": "Notification Configuration",
  "properties": {
    "channels": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["SMS", "EMAIL", "PUSH", "IN_APP"]
      },
      "description": "Notification channels"
    },
    "templates": {
      "type": "object",
      "description": "Notification templates",
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "documentation": {
    "description": "Configures notification channels, templates, and delivery settings.",
    "usage": "Define notification channels and templates for the service.",
    "examples": [
      {
        "channels": ["SMS", "EMAIL"],
        "templates": {
          "SUBMISSION": "Your application has been submitted successfully.",
          "APPROVAL": "Your application has been approved.",
          "REJECTION": "Your application has been rejected."
        }
      }
    ]
  }
} 